[2026-07-14T09:50:25.115Z] RESULT
============================================================
{
  "success": true,
  "output": "Audit done, cross-checked against current tree. 9 confirmed/new findings, no memory leaks found (no Combine/NotificationCenter in codebase, weak self already used).\n\n## 1. N+1 Queries\n\n**VoucherService.swift:1164** — one `findById` per account, every voucher post. Compounds inside `postBatch` loop (line 137-138).\n```swift\nfor id in ids {\n    guard let account = try accountRepo.findById(id) else { throw AppError.notFound(\"Account\") }\n```\nFix: add `AccountRepository.findByIds(_ ids: Set<Account.ID>) -> [Account.ID: Account]` using `WHERE id IN (...)`, same pattern as existing `findByCodes`.\n\n**BOMService.swift:75-87** — one `findItem` per BOM component in `validateBOMInput`.\n\n**ItemInvoiceService.swift:93-99** — one `findItemById` per invoice line, plus each line separately triggers full stock-movement replay (see #5).\n\n**BOMRepository.swift:33-52** `replaceComponents` — one INSERT per component in loop. Minor, batch-insert it.\n\n## 2. SwiftUI Re-renders\n\n**AccountPicker.swift:147-170** — `sortedAccounts` uncached computed property, re-sorts/re-filters full account list on every keystroke.\n```swift\nprivate var sortedAccounts: [Account] {\n    let base = filter.map { f in accounts.filter(f) } ?? accounts\n    return base.sorted { ... }\n}\n```\nFix: cache with `@State private var cachedSorted: [Account]`, recompute only in `.onChange(of: accounts)`, not on `query` change.\n\n## 3. Caching Gaps\n\n**ReportService.swift** — 4 of 10 report functions use `ReportCache` (ledger, trialBalance, profitAndLoss, balanceSheet). `gstSummary`, `dayBook`, `outstanding`, `stockValuation`, `cashFlow`, `stockAgeing` recompute every call, no cache key.\n\n**GSTView.swift:188-196** `GSTViewModel.reload()` calls `svc.summary()` then `svc.buildReturn()` — `buildReturn` internally re-calls `summary()` with same date range. Double query same call. Fix: pass computed summary into `buildReturn` instead of recomputing.\n\n## 4. Memory Leaks\n\nNone found. No Combine/NotificationCenter usage in codebase; `Task.detached` closures in view models already use `[weak self]`.\n\n## 5. Redundant Computations\n\n**BankReconciliationService.swift:96-119** — O(statement × vouchers) nested scan via `.first(where:)`, `matchedVoucherIds` Set never used to index.\n```swift\nfor s in statement {\n    if let v = vouchers.first(where: { v in ... }) { ... }\n}\n```\nFix: bucket vouchers by date/amount into dictionary first, O(n).\n\n**InventoryService.swift** — stock-out `recordMovement` replays full movement history **3x** per call (running balance line 191, authoritative movement line 193-197, republish valuation line 211-215 — unbounded history scan). `ItemInvoiceService.swift:175-185` calls `recordMovement` once per invoice line, so N lines = N×3 full replays. Worst offender in the set — fix first.\n\n## Priority\n\n1. InventoryService triple-replay (worst blast radius, compounds per line)\n2. VoucherService.loadAccounts N+1 (hit on every post/import)\n3. AccountPicker re-sort (hit on every keystroke, user-visible lag)\n4. ReportService uncached functions + GSTService double-query\n5. BOM/BankReconciliation (lower frequency paths)\n",
  "parsedOutput": {
    "sections": [
      {
        "title": "1. N+1 Queries",
        "content": "\n**VoucherService.swift:1164** — one `findById` per account, every voucher post. Compounds inside `postBatch` loop (line 137-138).\n```swift\nfor id in ids {\n    guard let account = try accountRepo.findById(id) else { throw AppError.notFound(\"Account\") }\n```\nFix: add `AccountRepository.findByIds(_ ids: Set<Account.ID>) -> [Account.ID: Account]` using `WHERE id IN (...)`, same pattern as existing `findByCodes`.\n\n**BOMService.swift:75-87** — one `findItem` per BOM component in `validateBOMInput`.\n\n**ItemInvoiceService.swift:93-99** — one `findItemById` per invoice line, plus each line separately triggers full stock-movement replay (see #5).\n\n**BOMRepository.swift:33-52** `replaceComponents` — one INSERT per component in loop. Minor, batch-insert it.\n\n",
        "level": 2
      },
      {
        "title": "2. SwiftUI Re-renders",
        "content": "\n**AccountPicker.swift:147-170** — `sortedAccounts` uncached computed property, re-sorts/re-filters full account list on every keystroke.\n```swift\nprivate var sortedAccounts: [Account] {\n    let base = filter.map { f in accounts.filter(f) } ?? accounts\n    return base.sorted { ... }\n}\n```\nFix: cache with `@State private var cachedSorted: [Account]`, recompute only in `.onChange(of: accounts)`, not on `query` change.\n\n",
        "level": 2
      },
      {
        "title": "3. Caching Gaps",
        "content": "\n**ReportService.swift** — 4 of 10 report functions use `ReportCache` (ledger, trialBalance, profitAndLoss, balanceSheet). `gstSummary`, `dayBook`, `outstanding`, `stockValuation`, `cashFlow`, `stockAgeing` recompute every call, no cache key.\n\n**GSTView.swift:188-196** `GSTViewModel.reload()` calls `svc.summary()` then `svc.buildReturn()` — `buildReturn` internally re-calls `summary()` with same date range. Double query same call. Fix: pass computed summary into `buildReturn` instead of recomputing.\n\n",
        "level": 2
      },
      {
        "title": "4. Memory Leaks",
        "content": "\nNone found. No Combine/NotificationCenter usage in codebase; `Task.detached` closures in view models already use `[weak self]`.\n\n",
        "level": 2
      },
      {
        "title": "5. Redundant Computations",
        "content": "\n**BankReconciliationService.swift:96-119** — O(statement × vouchers) nested scan via `.first(where:)`, `matchedVoucherIds` Set never used to index.\n```swift\nfor s in statement {\n    if let v = vouchers.first(where: { v in ... }) { ... }\n}\n```\nFix: bucket vouchers by date/amount into dictionary first, O(n).\n\n**InventoryService.swift** — stock-out `recordMovement` replays full movement history **3x** per call (running balance line 191, authoritative movement line 193-197, republish valuation line 211-215 — unbounded history scan). `ItemInvoiceService.swift:175-185` calls `recordMovement` once per invoice line, so N lines = N×3 full replays. Worst offender in the set — fix first.\n\n",
        "level": 2
      },
      {
        "title": "Priority",
        "content": "1. InventoryService triple-replay (worst blast radius, compounds per line)\n2. VoucherService.loadAccounts N+1 (hit on every post/import)\n3. AccountPicker re-sort (hit on every keystroke, user-visible lag)\n4. ReportService uncached functions + GSTService double-query\n5. BOM/BankReconciliation (lower frequency paths)",
        "level": 2
      }
    ],
    "codeBlocks": [
      {
        "language": "swift",
        "code": "for id in ids {\n    guard let account = try accountRepo.findById(id) else { throw AppError.notFound(\"Account\") }"
      },
      {
        "language": "swift",
        "code": "private var sortedAccounts: [Account] {\n    let base = filter.map { f in accounts.filter(f) } ?? accounts\n    return base.sorted { ... }\n}"
      },
      {
        "language": "swift",
        "code": "for s in statement {\n    if let v = vouchers.first(where: { v in ... }) { ... }\n}"
      }
    ]
  },
  "durationMs": 592575,
  "model": "sonnet",
  "sandboxMode": "permissive",
  "workerType": "optimize",
  "timestamp": "2026-07-14T09:50:25.115Z",
  "executionId": "optimize_1784022032540_16ijk5"
}
